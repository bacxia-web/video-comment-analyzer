# 隐私说明

Video & Comment Analyzer 是使用用户自己申请的服务密钥的 Chrome 扩展，没有开发者运营的数据服务器。

## 本地保存

DeepSeek、YouTube Data API、Supadata Key 保存于本机扩展的 `chrome.storage.local`，不使用 Chrome 同步存储。后台将此存储限制为 `TRUSTED_CONTEXTS`，普通网页和内容脚本无法读取。

Key 不会写入源码、导出内容或 GitHub。它们不是经过独立密码加密的保险库数据；有权访问本机浏览器资料的人可能读取本地存储。

哔哩哔哩、小红书和雪球的字幕、评论和结果在内存中临时保存，切换页面或关闭面板后不在面板中保留。小红书页面的扩展隔离环境会暂存本次已获取的评论，以便继续获取和去重，刷新该页面后清除；分析后台会暂存成功批次以便失败后重试，成功后删除，后台回收或浏览器关闭也会清除。获取范围设置保存于扩展本地存储。用户主动导出的 Markdown / JSON 文件可能包含公开评论者昵称和评论原文。YouTube 面板会在扩展本地存储保存字幕、摘要、评论结果、翻译缓存和笔记。

## 向外部服务发送什么

- **DeepSeek**：用户发起分析时，发送本次获取的字幕或评论、内容标题和创作者信息，以及用户输入的 DeepSeek Key 用于鉴权。小红书会分批分析全部已获取的评论，再发送各批议题摘要合并结果；重试会尽量复用仍在内存中的成功批次。字幕学习中的「AI 摘要」、中文或双语翻译、选中文字解释和笔记润色也会调用 DeepSeek。新视频默认显示原文，不会沿用上个视频的翻译模式自动调用 AI。选中文字直接「存为笔记」仅在本机保存，不调用 AI。
- **Google YouTube Data API**：采集 YouTube 评论时发送视频 ID、分页参数和用户配置的 Google Key。
- **Supadata**：仅当用户主动重新获取字幕、生成摘要、翻译或保存并润色片段，且网页字幕不可用、已配置可选服务时，才发送不含追踪参数的标准视频 URL 和 Supadata Key。仅打开 YouTube 面板不会调用 Supadata。字幕请求使用 `mode=native`。
- **YouTube / 哔哩哔哩**：读取网站已有字幕。B 站请求当前视频信息、当前分 P 的字幕列表及字幕内容，沿用用户在网站的登录状态，不提取或导出 Cookie。
- **小红书**：在当前笔记页面自动滚动、展开回复并读取可见评论，正常页面加载可能触发网站自己的请求。
- **雪球**：使用当前网站登录会话请求当前帖子的评论接口。

不向 DeepSeek、Google 或 Supadata 发送用户的网站 Cookie。API Key 不会传入执行于网页中的采集函数。没有遥测、广告追踪或后台自动分析。

## 控制与删除

设置页面可修改或清除 API Key；卸载插件可以清除该扩展的本地存储。下载的导出文件需要用户自行删除。网站登录由各网站管理，不受清除插件 Key 操作影响。

第三方 API 的数据保留与收费政策以服务商的说明为准。「只获取内容」不会发送内容给 DeepSeek，但 YouTube 评论 API 或配置过的备用字幕服务仍可能消耗相应服务额度。
